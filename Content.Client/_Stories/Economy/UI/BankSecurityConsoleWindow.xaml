<controls:FancyWindow
    xmlns="https://spacestation14.io"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
    Title="{Loc 'stories-bank-security-title'}"
    MinSize="850 550"
>
    <BoxContainer Orientation="Vertical" Margin="5">
        <TabContainer Name="Tabs" VerticalExpand="True">

            <BoxContainer Name="FinesTab" Orientation="Vertical" Margin="10" SeparationOverride="10">
                <controls:StripeBack>
                    <Label Text="{Loc 'stories-bank-security-fines-header'}" HorizontalAlignment="Center" Margin="5" />
                </controls:StripeBack>

                <BoxContainer Orientation="Vertical" VerticalExpand="True" Margin="0 0 0 10">
                    <Label Text="{Loc 'stories-bank-security-target-search'}" StyleClasses="LabelKeyText" />
                    <LineEdit Name="TargetSearchBar" PlaceHolder="{Loc 'stories-bank-security-target-search-ph'}" />
                    <PanelContainer VerticalExpand="True" MinHeight="200" Margin="0 5 0 0">
                        <PanelContainer.PanelOverride>
                            <gfx:StyleBoxFlat BackgroundColor="#1B1B1E" />
                        </PanelContainer.PanelOverride>
                        <ItemList Name="TargetItemList" />
                    </PanelContainer>
                </BoxContainer>

                <PanelContainer StyleClasses="LowDivider" />

                <GridContainer Columns="2" HorizontalExpand="True">
                    <Label
                        Text="{Loc 'stories-bank-security-amount'}"
                        StyleClasses="LabelKeyText"
                        VerticalAlignment="Center"
                    />
                    <SpinBox Name="AmountSpinBox" MinWidth="150" Margin="5 0 0 0" />

                    <Label
                        Text="{Loc 'stories-bank-security-reason'}"
                        StyleClasses="LabelKeyText"
                        VerticalAlignment="Center"
                        Margin="0 5 0 0"
                    />
                    <LineEdit Name="ReasonLineEdit" HorizontalExpand="True" Margin="5 5 0 0" />
                </GridContainer>

                <Button
                    Name="IssueFineButton"
                    Text="{Loc 'stories-bank-security-issue-fine'}"
                    StyleClasses="Caution"
                    HorizontalAlignment="Right"
                    MinWidth="150"
                />
            </BoxContainer>

            <BoxContainer Name="AccountsInfoTab" Orientation="Horizontal" Margin="10" SeparationOverride="10">
                <BoxContainer Orientation="Vertical" HorizontalExpand="True" SizeFlagsStretchRatio="1">
                    <LineEdit Name="AccSearchBar" PlaceHolder="{Loc 'stories-bank-security-target-search-ph'}" />
                    <PanelContainer VerticalExpand="True" Margin="0 5 0 0">
                        <PanelContainer.PanelOverride>
                            <gfx:StyleBoxFlat BackgroundColor="#1B1B1E" />
                        </PanelContainer.PanelOverride>
                        <ItemList Name="AccItemList" />
                    </PanelContainer>
                </BoxContainer>

                <PanelContainer StyleClasses="Inset" HorizontalExpand="True" SizeFlagsStretchRatio="1.5">
                    <BoxContainer Orientation="Vertical" Margin="10" SeparationOverride="10">
                        <Label
                            Text="{Loc 'stories-bank-security-acc-info-title'}"
                            StyleClasses="LabelHeading"
                            HorizontalAlignment="Center"
                        />
                        <PanelContainer StyleClasses="LowDivider" Margin="0 5" />
                        <GridContainer Columns="2" HSeparationOverride="10" VSeparationOverride="5">
                            <Label Text="{Loc 'stories-bank-security-acc-owner'}" StyleClasses="LabelKeyText" />
                            <Label Name="InfoOwnerLabel" Text="---" />

                            <Label Text="{Loc 'stories-bank-security-acc-number'}" StyleClasses="LabelKeyText" />
                            <Label Name="InfoNumberLabel" Text="---" StyleClasses="Monospace" />

                            <Label Text="{Loc 'stories-bank-security-acc-balance'}" StyleClasses="LabelKeyText" />
                            <Label Name="InfoBalanceLabel" Text="---" Modulate="#38b026" StyleClasses="LabelBig" />
                        </GridContainer>
                    </BoxContainer>
                </PanelContainer>
            </BoxContainer>

            <BoxContainer Name="LogsTab" Orientation="Vertical" Margin="10" SeparationOverride="5">
                <BoxContainer Orientation="Horizontal" SeparationOverride="5">
                    <LineEdit
                        Name="LogSearchBar"
                        PlaceHolder="{Loc 'stories-bank-security-log-search'}"
                        HorizontalExpand="True"
                    />
                    <Button Name="RefreshButton" Text="{Loc 'stories-bank-security-refresh'}" />
                </BoxContainer>

                <PanelContainer VerticalExpand="True">
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BackgroundColor="#1B1B1E" />
                    </PanelContainer.PanelOverride>
                    <ScrollContainer VerticalExpand="True" HorizontalExpand="True">
                        <GridContainer
                            Name="LogsGrid"
                            Columns="5"
                            HorizontalExpand="True"
                            Margin="5"
                            VerticalAlignment="Top"
                        >
                        </GridContainer>
                    </ScrollContainer>
                </PanelContainer>
            </BoxContainer>
        </TabContainer>

        <PanelContainer StyleClasses="LowDivider" Margin="0 5" />
        <BoxContainer Orientation="Horizontal" VerticalAlignment="Bottom" Margin="5">
            <Label Text="{Loc 'stories-bank-flavor-left'}" StyleClasses="WindowFooterText" />
            <Control HorizontalExpand="True" />
            <Label Text="{Loc 'stories-bank-flavor-right'}" StyleClasses="WindowFooterText" Margin="0 0 5 0" />
            <TextureRect
                StyleClasses="NTLogoDark"
                Stretch="KeepAspectCentered"
                SetSize="19 19"
                VerticalAlignment="Center"
                Margin="5 0 0 0"
            />
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
